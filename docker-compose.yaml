services:
  app:
    build: .
    container_name: document-formatter-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/storage:/app/app/storage
    restart: unless-stopped
